- @gps_location = request.location

= form_for @location do |f|
  = f.label :place
  = f.text_field :place, :autofocus => true, :value => @gps_location.city, :required => true

  = f.label :radius, "Area Radius (km)"
  = f.number_field :radius

  = f.submit